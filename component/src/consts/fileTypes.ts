import {FileType} from '../types/files';

export const ACCEPTED_FILE_EXTENSIONS: readonly FileType[] = ['csv', 'xls', 'xlsx', 'ods', 'txt'];

export const DEFAULT_FILE_EXTENSIONS: readonly FileType[] = ['csv'];
