import {FileFormat} from '../types/files';

export const ACCEPTED_FILE_FORMATS: readonly FileFormat[] = ['csv', 'xls', 'xlsx', 'ods', 'txt'];

export const DEFAULT_FILE_FORMATS: readonly FileFormat[] = ['csv'];
